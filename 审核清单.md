# App Store 上架审核检查清单

> 基于 Apple 官方《App Review Guidelines》逐条对照本工程（AC - 设备信息 Demo）整理。
> 用途：上架前自查。文档末尾有可勾选的行动清单。
> 相关章节：官方指南 Safety(1) / Performance(2) / Business(3) / Design(4) / Privacy & Legal(5)。

---

## 一、结论

本 App 属"设备信息展示"品类，可过审但存在多处会被 Review 追问或直接驳回的点。**代码层面最紧迫：删 IDFA、删推送、优化传感器耗电；合规层面：补隐私政策 + 审核备注说明；通过率最大变量：产品差异化（4.2/4.3）。**

---

## 二、必须改（高优先级，不改大概率被拒/被追问）

### 1. 删除 IDFA / ATT 追踪（Guideline 5.1.2(i)）
- **问题**：App 启动即调用 `requestIDFA()` 弹 ATT 追踪授权弹窗，但 App 不投放广告、不用 IDFA 做追踪。
- **指南**：5.1.2(i) 必须通过 App Tracking Transparency 获得明确许可才能跟踪用户；5.1.1(ii) 不得强制或误导用户同意不必要的数据访问。
- **动作**：
  - [ ] 删除 `requestIDFA()` / `refreshIDFA()` 方法
  - [ ] 删除 `import AdSupport`、`import AppTrackingTransparency`
  - [ ] 删除 Info.plist 的 `NSUserTrackingUsageDescription`
  - [ ] 删除 BundleInfoView 的"IDFA / 追踪授权"行
  - [ ] 删除 `@Published idfa / idfaAuthStatus`
  - [ ] 删除 README 中相关说明，改为"已移除"

### 2. 删除远程推送注册（Guideline 4.5.4 / 2.1(a)）
- **问题**：`registerForRemoteNotifications()` 已注册但无真实推送服务，`didFailToRegister` 在模拟器/无能力时必然触发，审核会质疑。
- **动作**：
  - [ ] 删除 `startMonitoring()` 中的 `UIApplication.shared.registerForRemoteNotifications()`
  - [ ] 删除 AppDelegate 的推送回调
  - [ ] 删除 `@Published pushToken / pushStatus` 及 View 行
  - [ ] 删除 `updatePushToken / updatePushError`

### 3. 补充隐私政策（Guideline 5.1.1(i)）
- **问题**：App 无隐私政策 URL，App Store Connect 必填，且须 App 内可访问。
- **动作**：
  - [ ] 准备隐私政策页面（可托管在自建网页）
  - [ ] 隐私政策须写明：收集哪些数据、如何收集、所有用途、共享的第三方（如 ipinfo.io）、保留/删除政策、如何撤销同意
  - [ ] App Store Connect 元数据填写隐私政策 URL
  - [ ] App 内"当前App"Tab 或设置区提供隐私政策链接（`SafariViewController`）

### 4. 定位权限说明与降级（Guideline 5.1.5 / 5.1.1(iv)）
- **问题**：请求精准定位用于展示 + SSID，但无"拒绝后的替代说明"，App 内也未明确"定位非必需"。
- **动作**：
  - [ ] App 内加说明文案："未授权定位不影响本 App 核心功能，仅定位/SSID 相关数据不可用"
  - [ ] 审核备注说明定位用途（展示设备定位能力 + WiFi SSID 需要）
  - [ ] 确认 `NSLocationWhenInUseUsageDescription` 文案与实际用途一致

### 5. 公共 API 合规（Guideline 2.5.1）
- **现状**：✅ 本工程全部使用公开 API（`resolv` 是公开 C 库，经 BridgingHeader 接入）。
- **动作**：
  - [ ] 保持只使用公开 API，不引入任何私有框架/符号
  - [ ] 确保运行在当前发行 OS（当前部署目标 15.6，需评估是否跟上）

---

## 三、建议打磨（中优先级）

### 6. 传感器/定位耗电优化（Guideline 2.4.2）
- **问题**：`CMMotionManager` 10Hz 常开、2s 轮询、定位持续更新，App 整个生命周期都在耗电，触碰"不得快速耗电/加重设备负担"。
- **动作**：
  - [ ] 仅在"传感器"Tab 可见时启动采样（`onAppear` 启动 / `onDisappear` 停止）
  - [ ] 定位改为 `requestLocation()` 单次请求而非持续 `startUpdatingLocation()`
  - [ ] 轮询间隔 2s → 5s

### 7. 数据准确性标注（Guideline 1.1.6）
- **问题**：内存估算（`used = total - free`）、CPU 使用率、`hw.machine → 机型名`映射为估算/可能不准；指南禁止"不准确的设备数据"。
- **动作**：
  - [ ] 估算字段标注"≈"
  - [ ] 机型映射表未命中时显示原始标识（已做）✅
  - [ ] 不夸大功能（描述中不写"医疗/诊断/测速"等）

### 8. 已装 App 探测说明（Guideline 5.1.2(iv)）
- **问题**：`canOpenURL` 探测已装 App，若被解读为"收集已装应用信息用于分析"会违规。
- **动作**：
  - [ ] 隐私政策声明：仅本地探测、不上传、不用于营销或分析
  - [ ] 审核备注说明用途（演示 scheme 探测能力）

### 9. 非显而易见功能说明（Guideline 2.3.1(a)）
- **问题**：越狱检测、scheme 探测、各种私有级探测属于"隐藏/非显而易见功能"。
- **动作**：
  - [ ] 审核备注逐项说明每个功能的用途
  - [ ] App 描述写清能力边界

### 10. 运动/健康数据（Guideline 5.1.3）
- **动作**：
  - [ ] 隐私标签如实勾选"运动与健身"（`CMMotionActivityManager` / `CMAltimeter` 数据）
  - [ ] 确认这些数据不出 App、不用于广告/营销（当前不出 App ✅）

### 11. 无障碍（HIG / Design）
- **动作**：
  - [ ] 支持动态字体（`@ScaledMetric` / 保持默认 SwiftUI 自适应）
  - [ ] 深色模式验证（已采集深色状态 ✅）
  - [ ] 对比度与 VoiceOver 基础适配

---

## 四、产品差异化（4.2 + 4.3，最可能被拒的点）

指南 4.3 明确点名"手电筒、壁纸、计时器"等**已饱和品类不接收无差异化 App**——设备信息类同理。纯展示易被判为低质/同质化。

- [ ] 增加差异化功能，例如：
  - 设备参数对比（同机型差异/历史趋势）
  - 数据报告导出（文本/PDF/截图分享）
  - 网络/传感器实时图表
  - 离线可用 + 参数解读
- [ ] 避免模板化观感（自定义分组、配色、图标）
- [ ] App 图标、截图、预览完整（截图展示实际使用场景，非启动页）

---

## 五、App Store Connect 提交前清单（官方 Before You Submit）

- [ ] 真机全功能回归（无崩溃、无闪退）
- [ ] 所有元数据完整准确（名称 ≤30 字符、副标题、关键词、分类 Utilities）
- [ ] 更新联系信息 / 支持 URL
- [ ] 年龄分级如实填写（本 App 预计 4+）
- [ ] 后端/外部服务审核期间可用（公网 IP 服务 ipinfo.io 等已实测可达 ✅）
- [ ] 审核备注详细说明非显而易见功能与各权限用途
- [ ] 截图与预览展示 App 实际使用
- [ ] 付费开发者账号（$99/年）
- [ ] 隐私标签（App Privacy）逐项填写（见下节）

---

## 六、隐私标签（App Privacy）填写对照表

本 App 采集的数据类型，需在 App Store Connect 隐私标签中如实申报：

| 数据类型 | 本 App 来源 | 是否收集/展示 | 说明 |
| --- | --- | --- | --- |
| 位置（精确/粗略） | `CLLocationManager` | 展示 | 需申报"Precise Location" |
| WiFi 信息 | `NEHotspotNetwork`（SSID/BSSID） | 展示 | 需申报 |
| 运动与健身 | `CMMotionActivityManager` / `CMAltimeter` | 展示 | 需申报"Health & Fitness" |
| 设备/系统信息 | UIDevice / sysctl / ProcessInfo / Metal 等 | 展示 | 需申报"Device ID"或"Other Data" |
| 网络 | NWPathMonitor / getifaddrs / DNS / 公网 IP | 展示 | 需申报；公网 IP 走第三方服务 ipinfo.io 等需披露 |
| 运营商 | `CTTelephonyNetworkInfo` | 展示 | 需申报 |
| 标识符 | `identifierForVendor` | 展示 | 需申报"Device ID" |
| 生物识别/密码状态 | `LAContext`（Face ID、是否设密码） | 展示 | 敏感，需如实申报 |
| 通知授权状态 | `UNUserNotificationCenter` | 展示 | 需申报 |
| 越狱检测 | 文件探测 | 展示 | 需说明 |

> 原则：所有数据仅本地展示、不上传（公网 IP 请求除外），隐私标签应勾选"用于 App 功能"而非"用于追踪"。

---

## 七、行动清单（自查用，逐项打勾）

### A. 代码改动
- [ ] 删除 IDFA / ATT 追踪
- [ ] 删除远程推送注册
- [ ] 传感器/定位按需采样（省电）
- [ ] 估算字段标注"≈"
- [ ] 定位非必需说明文案

### B. 内容与合规
- [ ] 隐私政策页面 + App 内链接 + App Store Connect 填 URL
- [ ] 隐私标签逐项申报（见第六节）
- [ ] 越狱检测、scheme 探测等写入审核备注
- [ ] 已装 App 探测用途写入隐私政策

### C. 产品打磨
- [ ] 差异化功能（对比/导出/图表，至少一项）
- [ ] App 图标、截图、预览
- [ ] 无障碍（动态字体、VoiceOver）

### D. 提交前
- [ ] 真机回归
- [ ] 元数据完整
- [ ] 年龄分级
- [ ] 联系信息
- [ ] 审核备注完整
- [ ] 付费开发者账号

---

*文档生成时间：2026-08-20。基于 Apple 官方 App Review Guidelines 对照本工程当前代码状态整理。*
